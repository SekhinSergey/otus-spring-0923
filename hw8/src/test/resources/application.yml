spring:
  data:
    mongodb:
      database: catalog
mongock:
  runner-type: "ApplicationRunner"
  change-logs-scan-package: ru.otus.spring.mongock
  mongo-db:
    write-concern:
      journal: false
    read-concern: local
  enabled: true